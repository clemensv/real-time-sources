name: USGS-IV Producer Tests

on:
  push:
    branches: [ "usgs-iv" ]
    paths:
      - 'usgs-iv/**'
      - '.github/workflows/test-usgs-iv.yml'
  pull_request:
    branches: [ "usgs-iv" ]
    paths:
      - 'usgs-iv/**'
      - '.github/workflows/test-usgs-iv.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./usgs-iv
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Poetry
        run: |
          curl -sSL https://install.python-poetry.org | python3 -
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Configure Poetry
        run: |
          poetry config virtualenvs.create true
          poetry config virtualenvs.in-project true

      - name: Cache Poetry dependencies
        uses: actions/cache@v4
        with:
          path: ./usgs-iv/.venv
          key: venv-${{ runner.os }}-${{ hashFiles('**/poetry.lock') }}
          restore-keys: |
            venv-${{ runner.os }}-

      - name: Install dependencies
        run: poetry install --no-interaction --no-ansi

      - name: Run unit tests
        run: poetry run pytest tests/test_usgs_unit.py -v --tb=short

      - name: Run integration tests
        run: poetry run pytest tests/test_usgs_integration.py -v --tb=short

      - name: Run all tests with coverage
        run: poetry run pytest tests/ -v --cov=usgs_iv --cov-report=term-missing --cov-report=xml --cov-fail-under=20

      - name: Upload coverage reports
        uses: codecov/codecov-action@v5
        if: always()
        with:
          file: ./usgs-iv/coverage.xml
          flags: usgs-iv
          name: usgs-iv-coverage
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
