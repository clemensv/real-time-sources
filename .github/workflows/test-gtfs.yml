name: GTFS Bridge Tests

on:
  push:
    branches: [ "main" ]
    paths:
      - 'feeders/gtfs/**'
      - '.github/workflows/test-gtfs.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'feeders/gtfs/**'
      - '.github/workflows/test-gtfs.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./feeders/gtfs
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Python 3.11
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov requests-mock
          pip install -e ./gtfs_rt_producer/gtfs_rt_producer_data
          pip install -e ./gtfs_rt_producer/gtfs_rt_producer_kafka_producer
          pip install -e ./gtfs_mqtt_producer/gtfs_mqtt_producer_data
          pip install -e ./gtfs_mqtt_producer/gtfs_mqtt_producer_mqtt_client
          pip install -e ./gtfs_amqp_producer/gtfs_amqp_producer_data
          pip install -e ./gtfs_amqp_producer/gtfs_amqp_producer_amqp_producer
          pip install -e ./gtfs_core
          pip install -e '.[dev]'

      - name: Run unit tests
        run: pytest tests/test_gtfs_bridge_unit.py -v --tb=short

      - name: Run integration tests
        run: pytest tests/test_gtfs_bridge_integration.py -v --tb=short

      - name: Run all tests with coverage
        run: pytest tests/ -v --cov=gtfs_rt_bridge --cov-report=term-missing --cov-report=xml --cov-fail-under=20

      - name: Upload coverage reports
        uses: codecov/codecov-action@v7
        if: always()
        with:
          file: ./feeders/gtfs/coverage.xml
          flags: gtfs
          name: gtfs-coverage
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
