[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tfl-road-traffic-amqp"
version = "0.1.0"
description = "AMQP companion feeder for TfL Road Traffic"
requires-python = ">=3.10"
dependencies = ["python-qpid-proton>=0.40.0"]

[project.scripts]
tfl-road-traffic-amqp = "tfl_road_traffic_amqp.app:main"

[tool.setuptools.packages.find]
include = ["tfl_road_traffic_amqp*"]
