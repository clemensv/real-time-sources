[pytest]
testpaths = tests
markers =
    unit: Unit tests
    integration: Integration tests with mocked HTTP
    e2e: End-to-end tests against live upstream

addopts = --tb=short --strict-markers

[coverage:run]
source = geosphere_austria
