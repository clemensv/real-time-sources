[pytest]
minversion = 6.0
addopts = -ra -v
testpaths = tests
markers =
    unit: Unit tests that don't require external dependencies
    integration: Integration tests that mock external dependencies
python_files = test_*.py
python_classes = Test*
python_functions = test_*
