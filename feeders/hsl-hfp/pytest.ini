[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests that do not require external services
    live: Tests that connect to the live HSL HFP broker (deselect with -m "not live")
addopts =
    -v
    --strict-markers
    --tb=short
