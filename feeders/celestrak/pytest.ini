[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests that don't require external services
    integration: Integration tests with mocked external dependencies
    e2e: End-to-end tests that call real external APIs
addopts = 
    -v
    --strict-markers
    --tb=short
    --cov=celestrak_core
    --cov=celestrak_kafka
    --cov=celestrak_mqtt
    --cov=celestrak_amqp
    --cov-report=term-missing
    --cov-report=html
