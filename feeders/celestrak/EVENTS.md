<!-- xreg-generator:hand-maintained - authored from xreg/celestrak.xreg.json for the CelesTrak source documentation. -->
# CelesTrak Events

[📘 **README**](README.md) &nbsp;·&nbsp;
[🐳 **Container contract**](CONTAINER.md) &nbsp;·&nbsp;
[📑 **Event schemas**](EVENTS.md) &nbsp;·&nbsp;
[🗄️ **KQL schema**](kql/celestrak.kql) &nbsp;·&nbsp;
[↗ **Upstream**](https://celestrak.org/)

Transport-neutral event family for public satellite orbital data redistributed by [CelesTrak](https://celestrak.org/), derived from the US Space Force public catalog. The SATCAT reference event and the GP / SupGP orbital telemetry events are keyed by the stable NORAD Catalog Number `{NORAD_CAT_ID}` so consumers can build temporally consistent views of one resident space object.

## At a glance

- **Event types:** 3 documented event types.
- **Transports:** KAFKA, MQTT/5.0, AMQP/1.0.
- **Reference vs telemetry:** 1 reference/catalog event type and 2 telemetry event types.
- **Identity:** `{NORAD_CAT_ID}` identifies the space object each event is about.
- **CloudEvents `source`:** the upstream CelesTrak URL the record was fetched from, including the configured GROUP or SupGP source.
- **Operations:** The bridge emits SATCAT reference data first at startup, refreshes it every `REFERENCE_REFRESH_INTERVAL`, and polls GP telemetry every `POLLING_INTERVAL`.
- **Read next:** [Quick start](#quick-start--how-to-consume), [Event catalog](#event-catalog), [Conventions](#conventions), [Operational notes](#operational-notes), [References](#references).

> [!NOTE]
> **About example payloads.** Example payloads below are illustrative, not orbital advice. Real payloads carry CelesTrak SATCAT / OMM values and CloudEvents IDs generated by the feeder.

## Quick start - how to consume

These examples show the smallest useful consumer for each transport declared by this source. Replace host names, credentials, topics, and addresses with your deployment values.

### Kafka

Subscribe to `celestrak`. The record key is `{NORAD_CAT_ID}`. In plain language, `{NORAD_CAT_ID}` is the stable identity of the object described by the event. Kafka uses the key for partition routing: events with the same key go to the same partition and keep per-key order, but consumers still receive an interleaved stream.

```python
from confluent_kafka import Consumer
c = Consumer({'bootstrap.servers':'localhost:9092','group.id':'events-demo','auto.offset.reset':'earliest'})
c.subscribe(['celestrak'])
while True:
    m = c.poll(1.0)
    if m and not m.error(): print(m.key(), dict(m.headers() or []), m.value())
```

Use different `group.id` values when every consumer should see every event; use the same group id to share partitions. Disable auto-commit and commit after processing for at-least-once application handling.

### MQTT 5

Connect to `mqtt://localhost:1883` and subscribe to `space/celestrak/satcat/+`, `space/celestrak/gp/+`, and `space/celestrak/supgp/+`. In MQTT filters, `+` matches exactly one topic level and `#` matches the remaining levels only when it is the final segment. Messages published with the RETAIN flag are delivered once per matching topic at subscribe time as Last Known Value; non-retained messages are live stream updates only.

```python
import paho.mqtt.client as mqtt
c = mqtt.Client(mqtt.CallbackAPIVersion.VERSION2, protocol=mqtt.MQTTv5)
c.on_message = lambda c,u,m: print(m.topic, getattr(m.properties,'UserProperty',None), m.payload)
c.connect('localhost', 1883)
c.subscribe([('space/celestrak/satcat/+',1),('space/celestrak/gp/+',1),('space/celestrak/supgp/+',1)])
c.loop_forever()
```

Subscribe at QoS 1 with a stable client id, `CleanStart=false`, and a finite non-zero session expiry when you need at-least-once delivery across reconnects. Retained messages are delivered subject to MQTT 5 Retain Handling. MQTT 5 user properties carry CloudEvents metadata.

### AMQP 1.0

Attach a link with `role=receiver` whose **source** is `celestrak`. The source terminus is the broker-side node you consume from. Generic brokers use SASL PLAIN over TLS, EXTERNAL with mTLS, or ANONYMOUS on trusted links. Azure Service Bus and Event Hubs can use CBS `put-token` on `$cbs` to install and refresh Entra ID JWTs or SAS tokens for long-lived AMQP connections.

```python
from proton.handlers import MessagingHandler
from proton.reactor import Container
class H(MessagingHandler):
    def on_start(self,e):
        conn=e.container.connect('amqps://<user>:<password>@<broker-host>:5671')
        e.container.create_receiver(conn,'celestrak')
    def on_message(self,e): print(e.message.subject, e.message.properties, e.message.body)
Container(H()).run()
```

The examples use AMQP binary content mode: the JSON payload is the message body, `datacontenttype` maps to the AMQP `content-type`, and CloudEvents attributes map to application properties named `cloudEvents:<attribute>`.

## Event catalog

### Satellite Catalog Entry

CloudEvents type: `org.celestrak.SatelliteCatalogEntry`

#### What it tells you

Reference record for one object in the US Space Force satellite catalog (SATCAT), as published by CelesTrak at `satcat/records.php`. Carries the object's identity (name, international designator, NORAD number), classification (type, owner, operational status) and coarse orbital parameters (period, inclination, apogee, perigee). Emitted as reference data at bridge start-up and periodically refreshed, so consumers can resolve the `NORAD_CAT_ID` on every OrbitMeanElements event to a named, described object without an out-of-band lookup. Shares the `NORAD_CAT_ID` key model and Kafka topic with the element-set events for temporal consistency.

#### Identity

Each event identifies the real-world object with `{NORAD_CAT_ID}`, the NORAD Catalog Number assigned to the tracked resident space object.

#### CloudEvents envelope

| Attribute | Value |
| --- | --- |
| `type` | `org.celestrak.SatelliteCatalogEntry` |
| `source` | `{feedurl}` - the SATCAT URL, for example `https://celestrak.org/satcat/records.php?GROUP=stations&FORMAT=json` |
| `subject` | `{NORAD_CAT_ID}` - the NORAD Catalog Number |
| `datacontenttype` | `application/json` |

#### Where to find it

| Transport | Location |
| --- | --- |
| `KAFKA` | topic `celestrak`, key `{NORAD_CAT_ID}` |
| `MQTT/5.0` | topic `space/celestrak/satcat/{NORAD_CAT_ID}`, retain `true`, QoS `1` |
| `AMQP/1.0` | source address `celestrak`, message subject `{NORAD_CAT_ID}`, application property `data_kind=satcat` |

#### Payload

`Satellite Catalog Entry` payloads are JSON objects. Required fields: `NORAD_CAT_ID`.

| Field | JsonStructure type | Unit / symbol | Description |
|---|---|---|---|
| `OBJECT_NAME` | `null or string` | - | Satellite / object name(s). May include alternate names in parentheses, an object-descriptor in brackets (e.g. `BREEZE-M DEB [TANK]`), and `&` to denote two or more attached objects. Mutable; never use as a key. From the SATCAT `OBJECT_NAME` field. |
| `OBJECT_ID` | `null or string` | - | International Designator (COSPAR ID), format `YYYY-NNNAAA`: launch year, launch number of the year, and one-to-three-letter piece designator (excluding I and O). Null for analyst objects with no associated launch. From the SATCAT `OBJECT_ID` field. |
| `NORAD_CAT_ID` | `int32` | - | NORAD Catalog Number: the globally-recognised unique identifier for a tracked resident space object, assigned by the US Space Force 18th Space Defense Squadron. One to nine digits (the five-digit range was exhausted at 100000+ on 2026-07-11; analyst objects use the 7995xxxxx range). Used as the CloudEvents `subject`, the Kafka partition key, the MQTT topic leaf and the AMQP subject. References the same object as the OrbitMeanElements event with the identical `NORAD_CAT_ID`. From the SATCAT `NORAD_CAT_ID` field. |
| `OBJECT_TYPE` | `null or ObjectTypeEnum` | - | Object type: `PAY` payload, `R/B` rocket body, `DEB` other debris, `UNK` unknown. From the SATCAT `OBJECT_TYPE` field. |
| `OPS_STATUS_CODE` | `null or string` | - | Operational status code: `+` operational, `-` nonoperational, `P` partially operational, `B` backup/standby, `S` spare, `X` extended mission, `D` decayed, `?` unknown. An object is considered 'active' if its status is one of `+`,`P`,`B`,`S`,`X`. Modelled as a plain string because the code symbols (`+`,`-`,`?`) are not valid enumeration identifiers. From the SATCAT `OPS_STATUS_CODE` field; see https://celestrak.org/satcat/status.php. |
| `OWNER` | `null or string` | - | Owner / source: the country or organisation that owns the object, as a CelesTrak source code (e.g. `US`, `CIS`, `PRC`, `ESA`, `ISS`, `TBD`). From the SATCAT `OWNER` field; see https://celestrak.org/satcat/sources.php. |
| `LAUNCH_DATE` | `null or string` | - | Launch date as a UTC calendar date, an ISO 8601 `YYYY-MM-DD` string. Null if unknown. From the SATCAT `LAUNCH_DATE` field. |
| `LAUNCH_SITE` | `null or string` | - | Launch site code (e.g. `TYMSC` Baikonur/Tyuratam, `AFETR` Cape Canaveral, `FRGUI` Kourou). Null if unknown. From the SATCAT `LAUNCH_SITE` field; see https://celestrak.org/satcat/launchsites.php. |
| `DECAY_DATE` | `null or string` | - | Decay (re-entry) date as a UTC calendar date, an ISO 8601 `YYYY-MM-DD` string. Null while the object is still on orbit. From the SATCAT `DECAY_DATE` field (upstream empty string is mapped to null). |
| `PERIOD` | `null or double` | - | Orbital period, in minutes. Null when no current elements are available. From the SATCAT `PERIOD` field. |
| `INCLINATION` | `null or double` | ° | Orbital inclination to the equator, in degrees (0-180). Null when no current elements are available. From the SATCAT `INCLINATION` field. |
| `APOGEE` | `null or int32` | - | Apogee altitude above the Earth's surface, in kilometres (the farthest orbital point). Null when no current elements are available. From the SATCAT `APOGEE` field. |
| `PERIGEE` | `null or int32` | - | Perigee altitude above the Earth's surface, in kilometres (the closest orbital point). Null when no current elements are available. From the SATCAT `PERIGEE` field. |
| `RCS` | `null or double` | - | Radar Cross Section, in square metres -- a proxy for the object's size as seen by tracking radar. Null when not available. From the SATCAT `RCS` field. |
| `DATA_STATUS_CODE` | `null or DataStatusCodeEnum` | - | Data status: `NCE` no current elements, `NIE` no initial elements, `NEA` no elements available. Null when elements are available. From the SATCAT `DATA_STATUS_CODE` field (upstream empty string is mapped to null). |
| `ORBIT_CENTER` | `null or string` | - | Orbit center: `EA` Earth, `EM` Earth-Moon barycenter, `EL1`/`EL2` Earth-Sun Lagrange points, `MO` Moon, `SU` Sun, `SS` solar-system escape, planetary bodies (`MA` Mars, `JU` Jupiter, `VE` Venus, ...), or a NORAD Catalog Number for an object docked to another catalogued object. Modelled as a plain string because the value space includes numeric catalog numbers. From the SATCAT `ORBIT_CENTER` field. |
| `ORBIT_TYPE` | `null or OrbitTypeEnum` | - | Orbit type: `ORB` orbiting, `LAN` landing, `IMP` impact, `DOC` docked to another catalogued object, `R/T` roundtrip. From the SATCAT `ORBIT_TYPE` field. |

#### Reference vs telemetry

This is reference/catalog data. Consumers should cache it and use it to interpret GP and SupGP telemetry events that share the same identity. MQTT retains the latest SATCAT copy per exact object topic.

### Orbit Mean Elements

CloudEvents type: `org.celestrak.OrbitMeanElements`

#### What it tells you

A General Perturbations (GP) orbital element set for one catalogued object, as published by CelesTrak at `NORAD/elements/gp.php`. This is a CCSDS Orbit Mean-Elements Message (OMM, CCSDS 502.0-B-3) fit by the US Space Force 18th Space Defense Squadron to Space Surveillance Network observations for use with the SGP4 propagator. The property names are the verbatim OMM keys, so the payload is a drop-in OMM JSON object for SGP4 libraries. Emitted whenever the bridge observes a new or refreshed element set for the object (GP data updates several times per day). Keyed by `NORAD_CAT_ID`.

#### Identity

Each event identifies the real-world object with `{NORAD_CAT_ID}`.

#### CloudEvents envelope

| Attribute | Value |
| --- | --- |
| `type` | `org.celestrak.OrbitMeanElements` |
| `source` | `{feedurl}` - the GP URL, for example `https://celestrak.org/NORAD/elements/gp.php?GROUP=stations&FORMAT=json` |
| `subject` | `{NORAD_CAT_ID}` - the NORAD Catalog Number |
| `time` | the normalized `EPOCH` value when available |
| `datacontenttype` | `application/json` |

#### Where to find it

| Transport | Location |
| --- | --- |
| `KAFKA` | topic `celestrak`, key `{NORAD_CAT_ID}` |
| `MQTT/5.0` | topic `space/celestrak/gp/{NORAD_CAT_ID}`, QoS `1` |
| `AMQP/1.0` | source address `celestrak`, message subject `{NORAD_CAT_ID}`, application property `data_kind=gp` |

#### Payload

`Orbit Mean Elements` payloads are JSON objects. Required fields: `EPOCH`, `MEAN_MOTION`, `ECCENTRICITY`, `INCLINATION`, `RA_OF_ASC_NODE`, `ARG_OF_PERICENTER`, `MEAN_ANOMALY`, `EPHEMERIS_TYPE`, `CLASSIFICATION_TYPE`, `NORAD_CAT_ID`, `ELEMENT_SET_NO`, `REV_AT_EPOCH`, `BSTAR`, `MEAN_MOTION_DOT`, `MEAN_MOTION_DDOT`.

| Field | JsonStructure type | Unit / symbol | Description |
|---|---|---|---|
| `OBJECT_NAME` | `null or string` | - | Satellite / object name (TLE Line 0). Up to 24 characters in the legacy TLE format; Unicode-capable in OMM. Mutable and not globally unique -- never use as a key. Null for uncatalogued / analyst objects that have no assigned name. From the CCSDS OMM `OBJECT_NAME` keyword. |
| `OBJECT_ID` | `null or string` | - | International Designator (COSPAR ID) of the object, format `YYYY-NNNAAA`: four-digit launch year, three-digit launch number of that year, and one-to-three-letter piece designator (letters I and O are excluded). Null for analyst objects with no associated launch. From the CCSDS OMM `OBJECT_ID` keyword. |
| `EPOCH` | `datetime` | - | UTC epoch of the element set: the reference instant to which every mean element below applies and from which an SGP4 propagator integrates. The upstream CCSDS OMM `EPOCH` keyword is an offset-less ISO 8601 value (UTC is implied by `TIME_SYSTEM=UTC`); the feeder normalizes it to a UTC-aware RFC 3339 timestamp with an explicit `+00:00` offset, e.g. `2026-07-15T18:23:37.536288+00:00`. From the CCSDS OMM `EPOCH` keyword. |
| `MEAN_MOTION` | `double` | rev/day | Mean motion: the number of orbital revolutions the object completes per solar day, a Brouwer mean element consumed directly by the SGP4 propagator. Larger values correspond to lower orbits (~15.5 for the ISS in LEO, ~1.0 for geostationary objects). From the CCSDS OMM `MEAN_MOTION` keyword. |
| `ECCENTRICITY` | `double` | - | Orbital eccentricity, dimensionless: 0 is a circular orbit and values approaching 1 are highly elliptical. In the legacy TLE this is stored as a leading-decimal integer; the OMM JSON gives the true fractional value. From the CCSDS OMM `ECCENTRICITY` keyword. |
| `INCLINATION` | `double` | deg (°) | Inclination of the orbital plane to Earth's equatorial plane, in degrees (0-180; > 90 indicates a retrograde orbit). From the CCSDS OMM `INCLINATION` keyword. |
| `RA_OF_ASC_NODE` | `double` | deg (°) | Right Ascension of the Ascending Node (RAAN), in degrees (0-360): the angle from the vernal equinox to the point where the orbit crosses the equatorial plane going north. From the CCSDS OMM `RA_OF_ASC_NODE` keyword. |
| `ARG_OF_PERICENTER` | `double` | deg (°) | Argument of pericenter (perigee), in degrees (0-360): the angle from the ascending node to the point of closest approach, measured in the orbital plane. From the CCSDS OMM `ARG_OF_PERICENTER` keyword. |
| `MEAN_ANOMALY` | `double` | deg (°) | Mean anomaly at epoch, in degrees (0-360): the fraction of the orbital period elapsed since pericenter passage expressed as an angle, used with the other elements to locate the object at `EPOCH`. From the CCSDS OMM `MEAN_ANOMALY` keyword. |
| `EPHEMERIS_TYPE` | `int32` | - | Ephemeris / orbital-model type. `0` denotes the SGP4 (SGP4/SDP4) mean-element theory, the only value distributed in public GP data. From the CCSDS OMM `EPHEMERIS_TYPE` keyword. |
| `CLASSIFICATION_TYPE` | `ClassificationTypeEnum` | - | Security classification of the element set (always `U` for public CelesTrak data). From the CCSDS OMM `CLASSIFICATION_TYPE` keyword. |
| `NORAD_CAT_ID` | `int32` | - | NORAD Catalog Number: the globally-recognised unique identifier for a tracked resident space object, assigned by the US Space Force 18th Space Defense Squadron. One to nine digits -- regular objects exhausted the five-digit range (reaching 100000+) on 2026-07-11, and analyst objects use the 7995xxxxx range -- so this MUST be treated as an up-to-9-digit integer, never a fixed-width field. Used as the CloudEvents `subject`, the Kafka partition key, the MQTT topic leaf and the AMQP message subject for every event this source emits. From the CCSDS OMM `NORAD_CAT_ID` keyword. |
| `ELEMENT_SET_NO` | `int32` | - | Element set number: a counter incremented (modulo 1000) each time a new element set is generated for the object; 18 SDS frequently emits the placeholder `999`. From the CCSDS OMM `ELEMENT_SET_NO` keyword. |
| `REV_AT_EPOCH` | `int32` | - | Revolution number at epoch: the integer count of orbital revolutions the object has completed since launch, as of `EPOCH`. From the CCSDS OMM `REV_AT_EPOCH` keyword. |
| `BSTAR` | `double` | 1/ER | B* (BSTAR) drag term, in inverse Earth radii, used by SGP4 to model the effect of atmospheric drag. Physically meaningful only for low-Earth-orbit objects; may be zero (deep space) or negative. From the CCSDS OMM `BSTAR` keyword. |
| `MEAN_MOTION_DOT` | `double` | rev/day^2 | First time-derivative of the mean motion (one half of dn/dt), in revolutions per day squared -- the classical TLE 'ballistic coefficient' term. From the CCSDS OMM `MEAN_MOTION_DOT` keyword. |
| `MEAN_MOTION_DDOT` | `double` | rev/day^3 | Second time-derivative of the mean motion (one sixth of d2n/dt2), in revolutions per day cubed; almost always 0 under the SGP4 theory. From the CCSDS OMM `MEAN_MOTION_DDOT` keyword. |

#### Reference vs telemetry

This is telemetry/event data. Treat each event as the current GP element set for an object at its `EPOCH`, suitable for SGP4 propagation by downstream consumers.

### Supplemental Orbit Mean Elements

CloudEvents type: `org.celestrak.SupplementalOrbitMeanElements`

#### What it tells you

A Supplemental GP (SupGP) orbital element set for one catalogued object, as published by CelesTrak at `NORAD/elements/supplemental/sup-gp.php`. Structurally a CCSDS OMM (identical to OrbitMeanElements) plus a fit-quality `RMS` and a `DATA_SOURCE` provenance code. SupGP is provided by satellite operators and analysis centres (SpaceX, ISS, Intelsat, laser-ranging, GPS almanacs, ...), is typically fit forward from the epoch, and -- unlike standard GP -- may carry several element sets per object distinguished by `DATA_SOURCE` and `EPOCH`. Emitted only when supplemental sources are configured. Keyed by `NORAD_CAT_ID`.

#### Identity

Each event identifies the real-world object with `{NORAD_CAT_ID}`. `DATA_SOURCE` differentiates supplemental providers in the payload, but transport identity remains per object.

#### CloudEvents envelope

| Attribute | Value |
| --- | --- |
| `type` | `org.celestrak.SupplementalOrbitMeanElements` |
| `source` | `{feedurl}` - the SupGP URL/source, for example `https://celestrak.org/NORAD/elements/supplemental/sup-gp.php?...` |
| `subject` | `{NORAD_CAT_ID}` - the NORAD Catalog Number |
| `time` | the normalized `EPOCH` value when available |
| `datacontenttype` | `application/json` |

#### Where to find it

| Transport | Location |
| --- | --- |
| `KAFKA` | topic `celestrak`, key `{NORAD_CAT_ID}` |
| `MQTT/5.0` | topic `space/celestrak/supgp/{NORAD_CAT_ID}`, retain `true`, QoS `1` |
| `AMQP/1.0` | source address `celestrak`, message subject `{NORAD_CAT_ID}`, application property `data_kind=supgp` |

#### Payload

`Supplemental Orbit Mean Elements` payloads are JSON objects. Required fields: `EPOCH`, `MEAN_MOTION`, `ECCENTRICITY`, `INCLINATION`, `RA_OF_ASC_NODE`, `ARG_OF_PERICENTER`, `MEAN_ANOMALY`, `EPHEMERIS_TYPE`, `CLASSIFICATION_TYPE`, `NORAD_CAT_ID`, `ELEMENT_SET_NO`, `REV_AT_EPOCH`, `BSTAR`, `MEAN_MOTION_DOT`, `MEAN_MOTION_DDOT`.

| Field | JsonStructure type | Unit / symbol | Description |
|---|---|---|---|
| `OBJECT_NAME` | `null or string` | - | Satellite / object name as published by the supplemental data provider. May carry provider annotations absent from standard GP data, e.g. an ISS ephemeris segment tag `ISS [Segment 01]` or a CPF prediction-centre tag in brackets. Mutable; never use as a key. From the CCSDS OMM `OBJECT_NAME` keyword. |
| `OBJECT_ID` | `null or string` | - | International Designator (COSPAR ID) of the object, format `YYYY-NNNAAA`: four-digit launch year, three-digit launch number of that year, and one-to-three-letter piece designator (letters I and O are excluded). Null for analyst objects with no associated launch. From the CCSDS OMM `OBJECT_ID` keyword. |
| `EPOCH` | `datetime` | - | UTC epoch of the element set: the reference instant to which every mean element below applies and from which an SGP4 propagator integrates. The upstream CCSDS OMM `EPOCH` keyword is an offset-less ISO 8601 value (UTC is implied by `TIME_SYSTEM=UTC`); the feeder normalizes it to a UTC-aware RFC 3339 timestamp with an explicit `+00:00` offset, e.g. `2026-07-15T18:23:37.536288+00:00`. From the CCSDS OMM `EPOCH` keyword. |
| `MEAN_MOTION` | `double` | rev/day | Mean motion: the number of orbital revolutions the object completes per solar day, a Brouwer mean element consumed directly by the SGP4 propagator. Larger values correspond to lower orbits (~15.5 for the ISS in LEO, ~1.0 for geostationary objects). From the CCSDS OMM `MEAN_MOTION` keyword. |
| `ECCENTRICITY` | `double` | - | Orbital eccentricity, dimensionless: 0 is a circular orbit and values approaching 1 are highly elliptical. In the legacy TLE this is stored as a leading-decimal integer; the OMM JSON gives the true fractional value. From the CCSDS OMM `ECCENTRICITY` keyword. |
| `INCLINATION` | `double` | deg (°) | Inclination of the orbital plane to Earth's equatorial plane, in degrees (0-180; > 90 indicates a retrograde orbit). From the CCSDS OMM `INCLINATION` keyword. |
| `RA_OF_ASC_NODE` | `double` | deg (°) | Right Ascension of the Ascending Node (RAAN), in degrees (0-360): the angle from the vernal equinox to the point where the orbit crosses the equatorial plane going north. From the CCSDS OMM `RA_OF_ASC_NODE` keyword. |
| `ARG_OF_PERICENTER` | `double` | deg (°) | Argument of pericenter (perigee), in degrees (0-360): the angle from the ascending node to the point of closest approach, measured in the orbital plane. From the CCSDS OMM `ARG_OF_PERICENTER` keyword. |
| `MEAN_ANOMALY` | `double` | deg (°) | Mean anomaly at epoch, in degrees (0-360): the fraction of the orbital period elapsed since pericenter passage expressed as an angle, used with the other elements to locate the object at `EPOCH`. From the CCSDS OMM `MEAN_ANOMALY` keyword. |
| `EPHEMERIS_TYPE` | `int32` | - | Ephemeris / orbital-model type. `0` denotes the SGP4 (SGP4/SDP4) mean-element theory, the only value distributed in public GP data. From the CCSDS OMM `EPHEMERIS_TYPE` keyword. |
| `CLASSIFICATION_TYPE` | `ClassificationTypeEnum` | - | Security classification of the element set (always `U` for public CelesTrak data). From the CCSDS OMM `CLASSIFICATION_TYPE` keyword. |
| `NORAD_CAT_ID` | `int32` | - | NORAD Catalog Number: the globally-recognised unique identifier for a tracked resident space object, assigned by the US Space Force 18th Space Defense Squadron. One to nine digits -- regular objects exhausted the five-digit range (reaching 100000+) on 2026-07-11, and analyst objects use the 7995xxxxx range -- so this MUST be treated as an up-to-9-digit integer, never a fixed-width field. Used as the CloudEvents `subject`, the Kafka partition key, the MQTT topic leaf and the AMQP message subject for every event this source emits. From the CCSDS OMM `NORAD_CAT_ID` keyword. |
| `ELEMENT_SET_NO` | `int32` | - | Element set number: a counter incremented (modulo 1000) each time a new element set is generated for the object; 18 SDS frequently emits the placeholder `999`. From the CCSDS OMM `ELEMENT_SET_NO` keyword. |
| `REV_AT_EPOCH` | `int32` | - | Revolution number at epoch: the integer count of orbital revolutions the object has completed since launch, as of `EPOCH`. From the CCSDS OMM `REV_AT_EPOCH` keyword. |
| `BSTAR` | `double` | 1/ER | B* (BSTAR) drag term, in inverse Earth radii, used by SGP4 to model the effect of atmospheric drag. Physically meaningful only for low-Earth-orbit objects; may be zero (deep space) or negative. From the CCSDS OMM `BSTAR` keyword. |
| `MEAN_MOTION_DOT` | `double` | rev/day^2 | First time-derivative of the mean motion (one half of dn/dt), in revolutions per day squared -- the classical TLE 'ballistic coefficient' term. From the CCSDS OMM `MEAN_MOTION_DOT` keyword. |
| `MEAN_MOTION_DDOT` | `double` | rev/day^3 | Second time-derivative of the mean motion (one sixth of d2n/dt2), in revolutions per day cubed; almost always 0 under the SGP4 theory. From the CCSDS OMM `MEAN_MOTION_DDOT` keyword. |
| `RMS` | `null or double` | - | Root-mean-square of the orbit-determination fit residuals reported by the supplemental data provider -- a quality indicator for this element set. Present only in SupGP data (absent from standard 18 SDS GP data). From the CelesTrak SupGP `RMS` field. |
| `DATA_SOURCE` | `null or string` | - | Supplemental data source / provider code identifying who produced this element set and by which technique, e.g. `ISS-E` (ISS ephemeris), `SpaceX-E` (SpaceX ephemeris), `CPF` (consolidated laser-ranging predictions), `GPS-A` (GPS almanac), `Intelsat-E`. Unlike standard GP, a single object may have several SupGP element sets distinguished by `DATA_SOURCE` and `EPOCH`. From the CelesTrak SupGP `DATA_SOURCE` field. |

#### Reference vs telemetry

This is telemetry/event data. Treat each event as a provider-specific SupGP element set for an object at its `EPOCH`. SupGP is off by default and emitted only when `SUPGP_SOURCES` is configured.

## Conventions

CloudEvents is the envelope around each JSON payload. It supplies metadata such as `specversion` (`1.0`), `type`, `source`, `id`, `time`, and `subject`. For this source, `subject` is the stable routing identity `{NORAD_CAT_ID}`. This repository convention mirrors the same identity to Kafka message key, MQTT topic identity segments, and AMQP message `subject` or application properties. The AMQP link address identifies the stream as a whole, not an individual object.

Transport bindings carry CloudEvents metadata differently:

| Transport | CloudEvents metadata location | Payload location |
| --- | --- | --- |
| Kafka binary mode | Kafka headers named `ce_<attribute>` for CloudEvents attributes except `datacontenttype`; `datacontenttype` maps to Kafka `content-type` | Kafka record value |
| Kafka structured mode | Inside the JSON CloudEvent envelope, with content type `application/cloudevents+json`; batched mode is not used by this generator | Kafka record value |
| MQTT 5 binary mode | MQTT 5 user properties named by the CloudEvents attribute (`id`, `source`, `type`, `subject`, ...), as defined by the CloudEvents MQTT binding; no `ce_` prefix | PUBLISH payload |
| AMQP 1.0 binary mode | Application properties named `cloudEvents:<attribute>` except `datacontenttype`; `datacontenttype` maps to AMQP `content-type` and must not be duplicated as an application property | AMQP message body |

All payloads documented here are JSON. MQTT retained messages are Last Known Value snapshots: the broker stores the most recent retained message per exact topic and delivers it to new subscribers when their subscription matches that topic.

## Operational notes

- The bridge emits SATCAT reference data first at startup, then polls GP telemetry every `POLLING_INTERVAL` seconds.
- `REFERENCE_REFRESH_INTERVAL` controls how often SATCAT is re-fetched and re-emitted as reference data.
- `SUPGP_SOURCES` is empty by default. Set it to comma/space-separated source names such as `SpaceX`, `Planet`, or `OneWeb` to enable SupGP telemetry.
- The bridge keeps a dedupe/watermark state file (`STATE_FILE`) so repeated upstream records are not intentionally republished as new events.
- `EPOCH` is emitted as RFC3339 UTC. CelesTrak publishes an offset-less ISO epoch with UTC implied; the bridge attaches UTC before emitting.
- **SupGP retain overwrite:** `SupplementalOrbitMeanElements` is published to the retained MQTT topic `space/celestrak/supgp/{NORAD_CAT_ID}`. When multiple SupGP `DATA_SOURCE` element sets exist for the same object, the retained value holds the last one written per object.
- **SupGP Latest materialized view is per-object:** the KQL `SupplementalOrbitMeanElementsLatest` materialized view dedups by subject (`NORAD_CAT_ID`) only, so it holds the latest element set per object and collapses multiple `DATA_SOURCE` sets. The base table retains all rows.
- Delivery semantics across all three transports are **at-least-once with idempotency key `(source, id)`** from the CloudEvents envelope. Consumers should dedupe on that tuple if exactly-once semantics are required.

## References

- xRegistry manifest: [`xreg/celestrak.xreg.json`](xreg/celestrak.xreg.json)
- Source README: [`README.md`](README.md)
- Container deployment guide: [`CONTAINER.md`](CONTAINER.md)
- CelesTrak: <https://celestrak.org/>
- SATCAT records endpoint: <https://celestrak.org/satcat/records.php?GROUP=stations&FORMAT=json>
- GP data format documentation: <https://celestrak.org/NORAD/documentation/gp-data-formats.php>
- Azure Service Bus emulator: <https://learn.microsoft.com/azure/service-bus-messaging/test-locally-with-service-bus-emulator>
