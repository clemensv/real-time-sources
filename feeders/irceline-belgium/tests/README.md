# Tests for IRCELINE Belgium

These tests are source-local. They cover connection-string parsing, timestamp conversion, pagination, observation deduplication, and status-interval normalization without touching shared repo tests.
