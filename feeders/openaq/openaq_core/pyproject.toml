[build-system]
requires = ["poetry-core>=1.1.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "openaq-core"
version = "0.1.0"
description = "Shared acquisition code for the OpenAQ feeder"
packages = [{include = "openaq_core"}]

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
requests = ">=2.32.3"
