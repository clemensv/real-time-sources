[build-system]
requires = [
    "setuptools>=64",
    "setuptools-scm>=8",
]
build-backend = "setuptools.build_meta"

[project]
name = "paris-bicycle-counters-mqtt"
dynamic = [
    "version",
]
description = "Paris bicycle counters bridge to MQTT/UNS"
authors = [
    { name = "Clemens Vasters", email = "clemensv@microsoft.com" },
]
requires-python = ">=3.10,<4.0"
dependencies = [
    "cloudevents>=1.11.0,<2.0.0",
    "dataclasses_json>=0.6.7",
    "paris_bicycle_counters_core @ file:../paris_bicycle_counters_core",
    "paho-mqtt>=2.1.0",
    "requests>=2.32.3",
]

[project.scripts]
paris-bicycle-counters-mqtt = "paris_bicycle_counters_mqtt:main"

[tool.setuptools_scm]
root = "../../.."
version_scheme = "post-release"
local_scheme = "no-local-version"

[tool.setuptools]
packages = [
    "paris_bicycle_counters_mqtt",
]
