[build-system]
requires = ["poetry-core>=1.1.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "ptwc_tsunami_amqp"
version = "0.1.0"
description = "AMQP 1.0 companion feeder for ptwc-tsunami."
authors = ["Clemens Vasters <clemensv@microsoft.com>"]
packages = [{ include = "ptwc_tsunami_amqp" }]

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
python-qpid-proton = ">=0.40.0"
cloudevents = ">=1.11.0,<2.0.0"
dataclasses_json = ">=0.6.7"
avro = ">=1.11.3"
azure-identity = ">=1.17.0"
azure-core = ">=1.30.0"
ptwc_tsunami_amqp_producer_data = { path = "../ptwc_tsunami_amqp_producer/ptwc_tsunami_amqp_producer_data" }
ptwc_tsunami_amqp_producer_amqp_producer = { path = "../ptwc_tsunami_amqp_producer/ptwc_tsunami_amqp_producer_amqp_producer" }

[tool.poetry.scripts]
ptwc-tsunami-amqp = "ptwc_tsunami_amqp.app:main"
